---
- name: installing packages for python env
  become: yes
  apt:
    name:
      - python3-pip
      - git
    update_cache: yes

- name: Creates directory
  file:
    path: ./basic-http-server
    state: directory

- name: Read-write git checkout from github
  ansible.builtin.git:
    repo: https://github.com/raimmissimo1/Rock-Scissor-Paper-game.git
    dest: ./basic-http-server

- name: Run the hugo command locally
  connection: local
  shell: "pwd"
  register: localPath

- debug:
    msg: "{{ localPath.stdout }}"

- name: Execute the script
  shell: nohup python3 /home/raim2007/basic-http-server/script1.py &
